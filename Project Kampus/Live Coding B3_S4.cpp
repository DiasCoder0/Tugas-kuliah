#include <iostream>

int main() {
    // Menggunakan namespace std agar tidak perlu menulis std::cout atau std::cin
    using namespace std;

    cout << "===== Segitiga sama sisi =====\n\n";

    int tinggi;
    cout << "Masukkan tinggi segitiga yang diinginkan: ";
    cin >> tinggi;

    // Pengecekan jika input tidak valid (0 atau negatif)
    if (tinggi <= 0) {
        cout << "Tinggi segitiga tidak valid. Program berhenti." << endl;
        return 1; // Keluar dari program karena input salah
    }
    if (int i = 1;  i <= tinggi;i++ )

    return 0;
}